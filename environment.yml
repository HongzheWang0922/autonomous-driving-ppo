name: rl-driving
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy
  - matplotlib
  - pip:
    - stable-baselines3[extra]>=2.0.0
    - gymnasium>=0.28.0
    - highway-env>=1.8.0
    - tensorboard>=2.13.0
    - torch>=2.0.0
    - opencv-python
